<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.deledzis.localshare.presentation.screens.main.MainActivityViewModel" />

        <variable
            name="userViewModel"
            type="com.deledzis.localshare.presentation.screens.main.UserViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/nav_host_fragment"
            android:name="com.deledzis.localshare.presentation.base.InjectingNavHostFragment"
            android:layout_width="@dimen/size_0dp"
            android:layout_height="@dimen/size_0dp"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toTopOf="@id/bottom_nav"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:navGraph="@navigation/nav_graph" />

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav"
            android:layout_width="@dimen/size_0dp"
            android:layout_height="@dimen/size_64dp"
            app:backgroundTint="@color/gray_700"
            app:elevation="@dimen/size_24dp"
            app:itemIconTint="@color/color_bottom_navigation_item_icon"
            app:labelVisibilityMode="unlabeled"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:menu="@menu/bottom_nav_menu"
            app:visibility="@{userViewModel.user != null &amp;&amp; viewModel.inTopLevelFragment.first}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>